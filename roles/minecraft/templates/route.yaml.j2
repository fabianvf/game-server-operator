---

apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: jellyfin
  name: jellyfin
spec:
  host: {{ host | default(None) }}
  port:
    targetPort: web
  to:
    kind: Service
    name: jellyfin
